version: '3'

services:

  app:
    container_name: app
    build:
      context: ./gameroom
      dockerfile: dockerfile
    env_file:
      - ./gameroom.env
    ports:
      - "5000:5000"
    volumes:
      - ./gameroom/app:/gameroom/app
